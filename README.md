# ThreatIntel-Aggregator

A scalable Threat Intelligence Aggregator that collects CVEs and security news, designed to integrate with NotebookLM for analysis.

## Running with Docker

To run the application using Docker, ensure you have Docker and Docker Compose installed. Then, run the following command from the root directory:

```bash
docker-compose up
```
